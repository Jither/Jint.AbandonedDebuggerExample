function test(a, b)
{
	return a + b;
}

const x = test(1, 3);
const y = test(2, 4);
let z;

if (x < y)
{
	z = x * y;
}
else
{
	z = x - y;
}

while (true)
{
	const x = Math.random();
	const p = Math.PI;
	const result = x * p;
}
